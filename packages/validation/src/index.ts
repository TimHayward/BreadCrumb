/**
 * @breadcrumb/validation: confirms a parser result against Microsoft Graph,
 * or SharePoint's Graph-shaped v2.0 API with the browser's session, through
 * an injected client. Pure: no network of its own, no DOM, no Node built-ins.
 */
export * from './graphValidation.js';
export * from './types.js';
