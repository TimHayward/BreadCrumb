{
  "name": "@breadcrumb/extension",
  "version": "0.3.0",
  "private": true,
  "description": "Manifest V3 extension for Microsoft Edge that lifts SharePoint and OneDrive citations out of Copilot responses, resolves them with @breadcrumb/parser and confirms them with the browser SharePoint session.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/parser": "workspace:*",
    "@breadcrumb/validation": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "catalog:",
    "@types/jsdom": "catalog:",
    "esbuild": "catalog:",
    "jsdom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
