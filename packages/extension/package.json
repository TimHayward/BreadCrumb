{
  "name": "@breadcrumb/extension",
  "version": "0.2.0",
  "private": true,
  "description": "Manifest V3 Chromium extension that lifts SharePoint and OneDrive citations out of Copilot responses, resolves them with @breadcrumb/parser and submits selected items to the BreadCrumb API.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/parser": "workspace:*",
    "@breadcrumb/validation": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "catalog:",
    "@types/jsdom": "catalog:",
    "esbuild": "catalog:",
    "jsdom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
