{
  "name": "@breadcrumb/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Manifest V3 Chromium extension that lifts file citations from Copilot responses and resolves them with @breadcrumb/parser. M1 ships a build-only stub; BC-042 replaces it.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@breadcrumb/parser": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "catalog:",
    "esbuild": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
